apiVersion: v1
kind: Service
metadata:
  name: {{ include "sglang-model.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "sglang-model.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}